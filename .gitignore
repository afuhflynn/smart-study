# === Next.js Build and Cache ===
# Next.js build output directory
.next/
# Next.js server build cache
.next/cache/
# Next.js development server logs
.next/development-build-logs/
# Next.js export output directory (if using 'next export')
out/

# === Node Modules ===
# Node.js dependency directory
node_modules/

# === Environment Variables ===
# Local environment variables (specific to each developer's machine)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# General pattern for any .env file (e.g., .env.secret)
*.env

# === Dependency Lock Files & Debug Logs ===
# npm debug log
npm-debug.log*
# yarn debug log
yarn-debug.log*
# pnpm debug log
pnpm-debug.log*

# === IDE and Editor Specific Files ===

# --- Visual Studio Code ---
# VS Code directory for workspace-specific settings, tasks, launch configs, etc.
# IMPORTANT: Consider committing .vscode/settings.json, launch.json, extensions.json
# if you want to enforce specific project settings or recommended extensions for the team.
# Generally, ignore temporary files and user-specific configurations within .vscode.
.vscode/
# The following lines are more granular to allow committing useful .vscode files
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
# Ignore remaining temporary files or user-specific things inside .vscode/
.vscode/*
!.vscode/

# --- IntelliJ / WebStorm / IDEA ---
# IntelliJ IDEA project files (user-specific configuration, caches, etc.)
.idea/
# Individual IntelliJ project files (older formats, generally covered by .idea/)
*.iml
*.ipr
*.iws

# --- Sublime Text ---
# Sublime Text project and workspace files
*.sublime-project
*.sublime-workspace

# --- Atom ---
# Atom project and workspace files
.atom/

# --- Vim / Neovim ---
# Swap files
*.swp
*.swo
*.swn
# Backup files
*.bak
# Persistent undo files
*.un~

# --- Emacs ---
# Backup files
*~
# Lock files
.#*
# Auto-save files
#*#

# === Operating System Generated Files ===
# macOS Finder thumbnail cache
.DS_Store
# Windows thumbnail cache
Thumbs.db
# Windows desktop.ini files for custom folder icons
Desktop.ini
# Temporary files generated by various OS actions (often for network shares)
._*
# Crashes logs (e.g., from Node.js)
hs_err_pid*.log

# === Testing and Coverage ===
# Jest/Vitest/Cypress test coverage reports
coverage/
# Playwright test-results
test-results/

# === Linting and Formatting ===
# ESLint cache file
.eslintcache
# Prettier cache file (less common, but can appear)
.prettiercache

# === Storybook Output ===
# If you are using Storybook, this is its default static build output
storybook-static/

# === General Logs and Temporary Files ===
# All log files
*.log
logs/
# Temporary files and directories
tmp/
temp/

# === Docker related (if used) ===
# Docker build context temporary directory
.dockerignore
# Docker volumes directory (if generated locally)
.volume/

# === Other Common Ignored Files ===
# Compiled TypeScript files (if not transpiled by Next.js or you have custom tsconfig)
# Next.js typically compiles on the fly, but sometimes you might have a 'dist'
# folder for specific backend code or libraries.
dist/
build/
# Any files explicitly created for a local dev server
server.log

# local dev todos
task.

# editor files
.vscode

